# This rule is not used by the project agentanvil itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the agenttasks.agentanvil.com.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: agentanvil
    app.kubernetes.io/managed-by: kustomize
  name: agenttask-editor-role
rules:
- apiGroups:
  - agenttasks.agentanvil.com
  resources:
  - agenttasks
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - agenttasks.agentanvil.com
  resources:
  - agenttasks/status
  verbs:
  - get
